<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androexp.cookbook">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PostsAll"/>
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PostRecipe" />
        <activity android:name=".Register" />
        <activity
            android:name=".RecipeDes"
            android:label="@string/title_activity_recipe_des" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="com.parse.SERVER_URL"
            android:value="@string/server_url" />
        <meta-data
            android:name="com.parse.APPLICATION_ID"
            android:value="@string/app_id" />
        <meta-data
            android:name="com.parse.CLIENT_KEY"
            android:value="@string/client_key" />
    </application>

</manifest>